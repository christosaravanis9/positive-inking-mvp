/// <reference types="vite/client" />

/** Injected by web/vite.config.ts's `define` block, computed fresh from git at every config load (dev-server start or build) -- see web/src/dev/BuildIdentifier.tsx. */
declare const __GIT_COMMIT__: string;
declare const __GIT_BRANCH__: string;
